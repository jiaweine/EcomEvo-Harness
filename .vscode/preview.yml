apps:
  - name: EcomEvo Website
    root: .
    port: 8000
    run: python -m pip install -e . && python -m uvicorn ecomevo.api.app:app --host 0.0.0.0 --port 8000
    autoOpen: true
    autoPreview: true
    mainPort: true
